/*

public folder me -> div#id#root so everthing that we are creating is appled to root using javascript 

// to start serveer :::: npm start

// Single page application:
        NO reload 
        Page will have only one javascript 
        If in multipage website then server will sent  different file for evely link so there will be reloading
        React is spa
        Only update will happen in js wihich will save bandwidth, size, data transpotation

// Node module folder is not given to other as it contains all modukes we can istall 
    get it again by install npm/ as iit is stored in package-lock.json 

 
 6
 Props.
    









*/